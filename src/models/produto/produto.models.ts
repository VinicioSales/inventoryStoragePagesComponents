export interface Produtos {
    quantidade: Number,
    nomeProduto: string,
    centroCusto: string[]
    unidadeMedida: string[],
}

export interface Produto {
    quantidade: Number,
    nomeProduto: string,
    centroCusto: string
    unidadeMedida: string,
}